# Environment Configuration for Render Deployment

# Application Settings
ENVIRONMENT=production
DEBUG=False

# CORS Settings (Update with your Flutter app domain)
# For development, use "*". For production, use your actual domain.
ALLOWED_ORIGINS=*

# Server Settings (Render will override PORT automatically)
PORT=8000

# Optional: Add your Flutter app's domain here for production
# ALLOWED_ORIGINS=https://your-flutter-app.com,https://app.growmate.in
